/**
 * @file
 * Empty example of Javascript structure.
 */

(function (Drupal) {
  Drupal.behaviors.storypal_example = {
    attach: function (context) {
      console.log('Storypal JS loaded.');
    }
  }
});